{
  "name": "@lightcrm/telegram-bot",
  "version": "0.3.8",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/bot.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "smoke:telegram": "tsx src/smoke.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lightcrm/core": "workspace:*",
    "@lightcrm/orchestrator": "workspace:*",
    "@lightcrm/storage": "workspace:*",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
